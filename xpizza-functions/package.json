{
  "name": "xpizza-functions",
  "version": "1.7.3",
  "description": "X Pizza Delivery Cloud Functions — Make.com → Firebase + Web Push + KDS sync + Auto-assign + Acceptance timeout + Stacking + WhatsApp inbound/outbound",
  "main": "index.js",
  "scripts": {
    "test": "node pixelpay-charge.test.js && node pixelpay-confirm.test.js && node pixelpay-webhook.test.js && node pixelpay-cancel.test.js && node driver-push.test.js && node driver-ingest.test.js && node factura/pricing.test.js && node factura/eligibility.test.js && node factura/sync.test.js",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "sync:rules": "node -e \"require('fs').copyFileSync('../xpizza-reference/database.rules.json','database.rules.json')\"",
    "deploy:rules": "npm run sync:rules && firebase deploy --only database",
    "logs": "firebase functions:log",
    "vapid": "npx web-push generate-vapid-keys"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.0",
    "googleapis": "^144.0.0",
    "node-fetch": "^2.7.0",
    "web-push": "^3.6.7"
  },
  "private": true
}
