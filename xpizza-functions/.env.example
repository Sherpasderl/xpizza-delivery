# Copy this to .env (without .example), then replace with a long random string.
# Generate one with:  openssl rand -hex 32
# Or: any random 40+ character string. Make.com will use this as the bearer token.
MAKE_SECRET=replace_me_with_a_long_random_string_at_least_40_chars
